# Aplicación CloudDesk

Ejecutar `docker compose up --build` y abrir `http://localhost:8000`. El servicio `web` usa Python/Flask/Gunicorn y el servicio `db` usa PostgreSQL 16. Consultar `../docs/02-arquitectura-local.md` para instrucciones y decisiones.
