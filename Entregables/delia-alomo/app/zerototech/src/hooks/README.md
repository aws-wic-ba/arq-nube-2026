# hooks

Hooks de React reutilizables entre más de una feature (por ejemplo
`useMediaQuery`, `useLocalStorage`). Un hook que solo lo usa una
feature puntual vive dentro de esa carpeta de feature, no acá.
