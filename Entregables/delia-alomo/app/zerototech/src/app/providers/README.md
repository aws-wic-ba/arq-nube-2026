# app/providers

Acá van los providers de contexto que envuelven toda la aplicación
(por ejemplo, un futuro `ThemeProvider` o `RouterProvider`). Se
componen en `App.tsx` alrededor de `MainLayout`, sin modificar el
layout en sí.
