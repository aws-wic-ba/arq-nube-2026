/**
 * Tipos compartidos entre features. Todavía no hay ninguno definido:
 * cuando una feature necesite un tipo usado por otras, se sube acá.
 * Los tipos que solo importan a una feature deberían vivir dentro de
 * esa misma carpeta de feature.
 */
export {};
