# Copiar a .env y ajustar. El .env NO se sube al repositorio.
# En AWS estos valores no irian en un archivo: irian en AWS Secrets Manager
# o en Parameter Store, y los leeria la aplicacion al arrancar.

DB_USER=shibashop
DB_PASSWORD=shibashop123
DB_NAME=shibashopdb
COOKIE_SECRET=cambiar-por-una-cadena-larga-y-aleatoria
